{% extends 'base.html.twig' %}

{% block title %}Create Client{% endblock %}

{% block body %}
    <h1>Create Client</h1>

    {{ form_start(form) }}

        {{ form_row(form.clLabel) }}

        {{ form_row(form.clNom) }}
<div class="form-buttons">
        <button class="btn btn-success">Create</button>
        <a href="/client">
            <button type="button">Annuler</button>
        </a>
</div>
    {{ form_end(form) }}

{% endblock %}
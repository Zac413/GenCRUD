
<td>
        <ul>
                {% for {{TD}} in {{LC_ENTITY_NAME}}.{{TD}}s %}
                <li>{{ {{TD}} }}
                {% else %}
                <li>Any {{TD}}</li>
                {% endfor %}
        </ul>

</td>
